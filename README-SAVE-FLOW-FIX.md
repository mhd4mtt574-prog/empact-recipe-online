# Recipe edit save-flow fix

This build updates an existing recipe in place, writes it to `App_Data/app-data.json`, verifies the saved record, and redirects to the main Recipe Library after a successful save. Validation errors are now shown at the top of the edit form.
